select {
  width: 150px;
  position: relative;
  right: 60px;
}

#add {
  position: relative;
  top: -40px;
  left: 3px;
}

#remove {
  position: relative;
  right: 60px;  
}

button {
  background-color: orange;
}

